# ── SNIPPET: orca_input_template_generation/fixed_method_basis_singlepoint_template ─
# Scheduler:   any
# Tool:        orca
# Tested:      dft_energy_orca_multiwfn
# Notes: Template is embedded in run_orca_wbo.sh.
# ────────────────────────────────────────────────────────────

cat > "${MOL}.inp" <<EOF
! wB97X-V def2-TZVP def2/J defgrid3 RIJCOSX CHELPG Loewdin Mayer
%scf
  MaxIter 500
end
%pal
  nproc 4
end
%maxcore 2500
%base "${MOL}"
*xyzfile ${CHARGE} ${MULT} ${XYZ_FILE}
EOF
