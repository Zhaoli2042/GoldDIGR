name: paired_input_discovery
description: Discover candidate ligands by finding *.xyz files with matching *.db basenames; skip strays.
category: operational
interface:
  inputs:
  - working directory contents
  outputs:
  - list of basenames to process
invariants:
- Skip any xyz that does not have a same-basename .db file.
variants:
  Perovskite-ligand-screening_default:
    file: Perovskite-ligand-screening_default.sh
    lines: 22
    scheduler: any
    tool: any
    infrastructure:
      filesystem: shared_fs
    tested_by:
    - MD/step1.build_bulk_sims.py
    notes: This is embedded in the driver; extracted as the tested loop logic.
