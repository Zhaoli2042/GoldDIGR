name: imbalanced_class_handling_smote
description: Apply SMOTE oversampling before train/test split.
category: science
interface:
  inputs:
  - data
  - label
  outputs:
  - X_train
  - X_test
  - y_train
  - y_test
invariants:
- Oversample minority class with SMOTE(sampling_strategy='minority') before splitting.
variants:
  Perovskite-ligand-screening_default:
    file: Perovskite-ligand-screening_default.sh
    lines: 15
    scheduler: any
    tool: sklearn
    infrastructure:
      filesystem: shared_fs
    tested_by:
    - ML/ML_all_model.py
    notes: This is the exact ordering used in the workflow.
